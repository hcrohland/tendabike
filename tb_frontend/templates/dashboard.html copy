{% extends "base" %}

{% block content %}
    <table class="table table-striped">
        <tr>
            <th class="table-success"><h2>{{user.firstname}} {{user.name}}'s toys and spares </h2></th>
        </tr>
    </table>
{% for w, gears in gear | group_by(attribute="what") %}
    <div class="container">
        {% set type = types[gears[0].what] %}
        <table class="table table-striped table-hover">
            <tr>
                {{ macros::part_header(title=type.name) }}
            </tr>
            {% for s in gears %}
                <tr>
                    {{ macros::part_row(part=s) }}
                </tr>
            {% endfor %}
        </table> 
    </div>
    <div class="container">
    {% set_global first = true %}
    {% for s in  spares %}
    {% set type = types[s.what] %}
    {% if type.main != w %}
        {% contine %}
    {% endif%}
    {% if first %}
        {% set_global first = false %}

        <table class="table table-striped table-hover">
            <tr>
                <th>Spares</th>
                {{ macros::part_header() }}
                <th></th>
            </tr>
    {% endif %}

                {% for s in spares %}
                    <tr>
                        {% if len > 0 %}
                            <td rowspan={{len}} style="vertical-align:middle"> <a href="/type/{{ type.id }}"> {{type.name}} </a> </td>
                            {% set_global len = 0 %}
                        {% endif %}
                        {{ macros::part_row(part=s) }}
                        <td style="align-text:center">
                            <div class="dropdown">
                                <button type="button" class="btn-xs btn-secondary dropdown-toggle" data-toggle="dropdown"></button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" href="/part/attach/{{s.id}}">attach</a>
                                </div>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
            {% endfor %}
        </table> 
    {% endif %}
    </div>
{% endfor %}

{% endblock content %}